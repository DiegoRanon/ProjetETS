class Product {
  constructor(
    id,
    title,
    description,
    image,
    category,
    address

    
  ) {
    this.id = id;
    this.title = title;
    this.description = description;
    this.image = image;
    this.category = category;
    this.address = address;
  }
}

export default Product;
